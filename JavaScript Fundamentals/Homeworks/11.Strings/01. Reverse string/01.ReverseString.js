function ReverseString(args) {
    // body...
    var reversed = +args[0],
        output,
        i;
    // for (i = reversed.length - 1; i >= 0; i--) {
    //     reversed.push(args[i]);
    // }
    forEach( char in reversedSubstring){
      string.
    }


}
ReverseString(['sample']);
ReverseString(['qwertytrewq']);
